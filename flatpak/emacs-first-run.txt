This Emacs build runs inside the Freedesktop SDK Flatpak.

Commands found in the SDK run inside the Flatpak. To run a command from the
host explicitly, use:

  flatpak-spawn --host COMMAND

The application can access host files and the SSH/GPG agent sockets. Additional
SDK extensions can be enabled with FLATPAK_ENABLE_SDK_EXT.
